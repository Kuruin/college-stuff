## Packages
framer-motion | Smooth page transitions and micro-interactions
date-fns | Formatting dates for event display
lucide-react | Beautiful icons for the UI

## Notes
- Auth workflow: /api/login, /api/register, /api/user
- Role-based access: Admin can create/delete events, User can view/upload
- Media upload: Multipart form data to /api/events/:id/media
